# Database (PostgreSQL - Vercel Postgres)
# For Vercel: Get from Vercel Postgres dashboard
# For local: Use PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/optigenius"
DIRECT_URL="postgresql://user:password@localhost:5432/optigenius"

# NextAuth Configuration
# For production: Use your actual domain (e.g., https://optigenius.vercel.app)
# For local: Use http://localhost:3000
NEXTAUTH_URL="http://localhost:3000"
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# OpenAI API Key (required for AI insights - authenticated users only)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY="your_openai_api_key_here"

# OAuth Providers (Optional - for Google/GitHub sign-in)
# Google OAuth - Get credentials from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# GitHub OAuth - Get credentials from: https://github.com/settings/developers
GITHUB_ID="your-github-client-id"
GITHUB_SECRET="your-github-client-secret"
